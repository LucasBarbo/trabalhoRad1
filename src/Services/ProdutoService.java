package Services;

import java.util.List;

/**
 *
 * @author Wil
 */
/*public class ProdutoService {

    class ClienteService {

        private ProdutoDAO produtoDAO;

        public ProdutoService() {
            produtoDAO = new ProdutoDAO();
        }

        public void salvar(Produto produto) throws ServiceException {

            if (produto.getNomeProduto().isEmpty()) {
                throw new ServiceException(" Campo nome do produto é obrigatorio ");
            }
            if (produto.getDescricao().isEmpty()) {
                throw new ServiceException(" Campo descrição é obrigatorio ");
            }
            produtoDAO.salvar(produto);
        }

        public Produto buscarPorId(Integer id) {
            return produtoDAO.buscarPorId(id);
        }

        public void excluir(Integer id) {
            produtoDAO.excluir(id);
        }

        public List<Produto> buscarTodos() {
            return produtoDAO.buscarTodos();
        }
    }

}
*/